apiVersion: apps/v1
kind: Deployment
metadata:
  name: oke-infrastructure-server
  labels:
    app: oke-infrastructure-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oke-infrastructure-server
  template:
    metadata:
      labels:
        app: oke-infrastructure-server
    spec:
      containers:
      - name: server
        image: oci-registry/oke-infrastructure-server:latest
        ports:
        - containerPort: 8080
        env:
        - name: SERVER_PORT
          value: "8080"
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: data
          mountPath: /app/server/data
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: oke-infrastructure-server-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oke-infrastructure-server-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: oci-bv
