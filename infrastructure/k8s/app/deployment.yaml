apiVersion: apps/v1
kind: Deployment
metadata:
  name: hineat-server
  labels:
    app: hineat-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hineat-server
  template:
    metadata:
      labels:
        app: hineat-server
    spec:
      containers:
      - name: server
        image: oci-registry/hineat-server:latest
        ports:
        - containerPort: 8080
        env:
        - name: SERVER_PORT
          value: "8080"
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: data
          mountPath: /app/server/data
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: hineat-server-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hineat-server-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: oci-bv
